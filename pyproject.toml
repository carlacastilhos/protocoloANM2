[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "Protocolo Digital ANM"
authors = [{name = "ANM", email = "gedoc@anm.gov.br"}]
dynamic = ["version", "description"]
